{
  "name": "getunstuck-proxy",
  "version": "1.0.0",
  "description": "GetUnstuck API proxy — rate limiting, OpenAI forwarding, Nope signal aggregation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
